# === IDEs y editores ===
.idea/
.vscode/
*.swp

# === Python ===
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/
build/
dist/
.python-version

# Entornos virtuales
.venv/
venv/
env/

# Testing y cobertura
.pytest_cache/
.coverage
htmlcov/
.tox/

# Type checkers y linters
.mypy_cache/
.ruff_cache/

# Jupyter
.ipynb_checkpoints/

# === Credenciales y configuración local ===
.env
*.local.yml
secrets/

# === Data Lake (Bronze/Silver/Gold) — datos generados, no se versionan ===
# Se ignora el contenido de data/ pero se conserva el esqueleto de carpetas (.gitkeep)
data/**
!data/**/
!data/**/.gitkeep
*.parquet

# === Spark / Delta Lake ===
spark-warehouse/
metastore_db/
derby.log
*.crc
_delta_log/

# === DuckDB (capa Gold) — catálogos/ficheros locales generados ===
*.duckdb
*.duckdb.wal

# === dbt ===
dbt/target/
dbt/dbt_packages/
dbt/logs/
logs/

# === Airflow (docker-compose) — artefactos generados, no se versionan ===
!docker/airflow/logs/
docker/airflow/logs/**
!docker/airflow/logs/**/
!docker/airflow/logs/**/.gitkeep
docker/airflow/config/airflow.cfg

# === Power BI (ficheros temporales) ===
*.pbix.tmp
~$*.pbix

# === Scala / SBT ===
/target/
project/target/
project/project/
.bloop/
.bsp/
.metals/
.ammonite/
metals.sbt

# === Documentación de trabajo (specs/planes de Claude Code) — no se versiona ===
docs/superpowers/

# === Sistema operativo ===
.DS_Store
Thumbs.db
.claude
.remember
/.superpowers/
