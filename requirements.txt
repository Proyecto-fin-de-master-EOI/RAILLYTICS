# === Ingesta (capa Bronze) ===
requests~=2.32          # Descarga de APIs REST (Renfe Open Data, AEMET, BOE)
python-dotenv~=1.0      # Carga de variables de entorno desde .env
pandas~=2.2             # Manipulación de datos en la ingesta
pyarrow~=16.1           # Lectura/escritura Parquet
PyYAML~=6.0              # Parseo de config/data_sources.yml

# === Data Lake (DuckDB) ===
duckdb~=1.5.5           # Acceso al lake desde Python (Silver sintético, trazabilidad, notebooks) y motor de consulta de Superset

# === Machine Learning ===
scikit-learn~=1.5       # Modelo predictivo de demanda
numpy~=1.26             # Dependencia base de scikit-learn

# === Exploración ===
jupyter~=1.0            # Notebooks de EDA
matplotlib~=3.9         # Visualización en notebooks

# === Testing ===
pytest~=8.2             # Tests unitarios de ingesta y transformaciones
requests-mock~=1.12      # Mock de requests para tests de descarga
